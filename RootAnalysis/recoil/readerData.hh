#ifndef READERDATA_h
#define READERDATA_h

struct ReaderData {
  float gmtauhad, gm2hadmax, gm2hadmin;
  float gmbr0pos, gmbr0neg, gpttaur0pos, gpttaur0neg;
  float gpttauhad;
  float gatauhad, gperptauhad, gparatauhad;
  float gpthad1, gpthad2, gpthad3;
  float gparanu, gperpnu;
};

#endif
