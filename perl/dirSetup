#!/usr/bin/env perl

use Getopt::Std;
getopts('n:');

system("ln -s ../plotResults.2016BF.files");
system("ln -s ../plotResults.2016GH.files");

system("/bin/cp ../cuts/baseCuts.2016.cuts .");

system("mkdir -p overlays/2016");
system("mkdir -p sbso");
system("mkdir -p adfpy");
system("mkdir -p sbsctrl");
